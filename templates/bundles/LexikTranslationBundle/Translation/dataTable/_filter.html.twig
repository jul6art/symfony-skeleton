<th>
    {% if column.filter and column.visible %}
        <input data-toggle="translation_table_filter" type="text" name="{{ column.index }}" value="{{ (app.request.get(column.index))|default('') }}" class="form-control input-sm" />
    {% endif %}
</th>
